# MedCite backend dependencies.
# The core (PDF reading, hybrid search, extractive answers) runs on just these:
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.6
PyMuPDF>=1.24            # reads PDFs and keeps the page number of every line
scikit-learn>=1.3        # TF-IDF semantic ranker
numpy>=1.24
requests>=2.31           # only for the sample-PDF fetch helper

# Optional: only needed if you set AI_API_KEY to have Groq write the answers.
# Without a key the backend uses the built-in extractive answerer.
groq>=0.11

# Optional upgrades mentioned in the design (not required to run):
#   chromadb>=0.5              # swap-in vector store
#   sentence-transformers>=3.0 # swap-in semantic embeddings
